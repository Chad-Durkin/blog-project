{{#if updateGameForm}}
    <h1>Edit {{game.name}}</h1>
      <div class="update-game">
        <form>
          <div class="form-group">
              <label for="name">Game Name</label>
              {{input value=name class="name" placeholder=game.name}}
          </div>
          <div class="form-group">
              <label for="description">Game Description</label>
              {{input value=description class="description" placeholder=game.description}}
          </div>
          <div class="form-group">
              <label for="playerCount">How many players can play this game?</label>
              {{input value=playerCount class="playerCount" placeholder=game.playerCount}}
          </div>
          <div class="form-group">
              <label for="image">Update the image link for this game</label>
              {{input value=image class="image" placeholder=game.image}}
          </div>

          <button {{action 'update' game}}>Save</button>
        </form>
      </div>
    {{else}}
      <button {{action 'updateGameForm'}}>Update</button>
    {{/if}}
